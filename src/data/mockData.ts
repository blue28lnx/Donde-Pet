// Datos seed removidos: la app arranca vacía y los usuarios publican
// sus propias mascotas una vez que inician sesión con Google.
export const MOCK_USERS: import('../types').User[] = [];
export const MOCK_PETS: import('../types').Pet[] = [];
export const MOCK_MESSAGES: import('../types').Message[] = [];
